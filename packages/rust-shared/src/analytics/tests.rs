#[cfg(test)]
mod tests {
    // Tests disabled due to API mismatch. TODO: Re-enable after fixing mod.rs
    /*
    use super::super::*;
    use mockall::{mock, predicate::*};
    use posthog_rs::Properties;
    ...
    */
}